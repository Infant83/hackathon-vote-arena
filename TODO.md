# TODO

이 문서는 행사 전까지 남은 작업을 우선순위별로 정리합니다.

## P0. 행사 전 반드시 확인

- [x] 2026-05-11 회의 결정사항 중 핵심 정책을 앱 기준으로 반영한다.
  - 사용자 인증 입력을 `이름 + Let's ID` 기준으로 바꾼다.
  - Let's ID에 사내 이메일을 입력하면 `@` 뒤 도메인은 제외하고 ID만 사용한다.
  - 같은 이름과 같은 Let's ID가 새 기기에서 들어오면 같은 참여자로 묶고, 새 device ID를 함께 등록한다.
  - 참가자 1인당 기본 별 개수를 10개로 변경한다.
  - 한 팀에 최대 10개까지 줄 수 있는 정책은 유지한다.
  - 점수 환산은 1등 10점, 최하위는 관리자 설정 점수, 중간 팀은 득표수 기준 선형 배분으로 변경한다.
  - 관객 송출 보드에는 심사용 환산점수를 노출하지 않고, 별 개수와 응원 댓글 중심으로 보여준다.
  - 투표자 이름이 실시간으로 노출되어 오해를 만들 수 있는 UI를 제거하거나 관리자 전용으로 제한한다.
- [x] 관객용 실시간 동기화 비용을 줄인다.
  - `/vote`는 SSE를 끄고 15초 polling 중심으로 동기화한다.
  - 투표/응원 메시지 전송 직후에는 POST 응답으로 즉시 갱신한다.
  - 투표 마감 후에는 관객 화면 polling을 중지한다.
  - `/admin`과 `/admin?showCheer=1`만 SSE를 유지한다.
- [ ] 관리자 화면 접근 보호를 추가한다.
  - 현재 `/admin` URL만 알면 관리자 화면에 접근할 수 있다.
  - 최소한 간단한 admin passcode 또는 Cloudflare Access 보호를 붙이는 것이 좋다.
- [ ] 실제 행사 팀 정보와 로고를 반영한다.
  - `team_infos.zip` 또는 `team_info.json`으로 업로드한다.
  - 팀 로고는 `512 x 512 px` 정사각형 기준으로 받는다.
- [ ] 행사 전 최종 Reset 절차를 정한다.
  - 리허설 후 `/admin`에서 `Reset`을 실행한다.
  - 테스트 데이터가 남아 있지 않은지 확인한다.
- [ ] 30명, 100명, 300명 규모의 리허설을 순차적으로 해본다.
  - 모바일 접속
  - 별 배분
  - 응원 메시지 작성
  - 관리자 SSE 연결 유지
  - Showup 화면 표시
  - 결과 XLSX 다운로드
- [ ] 사내망에서 `workers.dev` 접속이 차단되지 않는지 확인한다.
- [ ] 발표장 PC, 사회자 PC, 관리자 PC, 모바일 접속 주소를 운영 시나리오로 정리한다.
- [ ] 운영 중 긴급 복구 방법을 문서화한다.
  - Cloudflare Dashboard에서 Worker 확인
  - 수동 `npm run cf:deploy`
  - 로컬 Node 서버로 임시 전환할 경우의 접속 주소
- [ ] 결과 XLSX가 행사 운영자가 기대하는 형식인지 확인한다.
  - 팀별 결과
  - 참여자
  - 응원 메시지
  - 추첨 결과
- [ ] 행사 당일 운영 플로우를 1부/2부 추첨 기준으로 정리한다.
  - 1부 추첨 룰
  - 2부 추첨 룰
  - 사회자 또는 VIP가 `멈춤` 버튼을 누르는 흐름
  - 부적절 메시지 모니터링 담당자 지정

## P1. 안정성 강화

- [ ] Durable Object 상태 저장 구조를 더 명확히 versioning한다.
- [ ] D1 또는 외부 DB 기반 영구 저장소를 추가한다.
  - `teams`
  - `participants`
  - `votes`
  - `cheers`
  - `raffle_runs`
  - `admin_audit_logs`
- [ ] SSE broadcast를 역할별로 나눈다.
  - 관객용 이벤트
  - 관리자용 이벤트
  - Showup용 이벤트
- [ ] 사용자 이벤트를 모두에게 full state로 broadcast하지 않고 delta event 중심으로 줄인다.
- [ ] 관리자 작업 audit log를 남긴다.
  - 팀 정보 수정
  - 메시지 숨김/공개/삭제
  - Reset
  - 테스트 데이터 주입
  - 추첨 실행
- [ ] rate limit을 추가한다.
  - 등록 요청
  - 별 변경 요청
  - 응원 메시지 요청
- [ ] 응원 메시지 길이와 빈도 제한을 관리자 설정으로 뺀다.
- [ ] 메시지 유해어/금칙어 필터를 추가한다.
  - 자동 숨김 후보
  - 관리자 검토 후 공개

## P2. 운영 편의성

- [ ] 관리자 화면에 현재 room/session 정보를 명확히 표시한다.
- [ ] 관리자 화면에 `현재 설정 다운로드`와 `전체 상태 백업 다운로드`를 분리한다.
- [ ] 결과 내보내기에서 CSV도 함께 지원한다.
- [ ] 결과 XLSX에 간단한 스타일을 추가한다.
  - 헤더 bold
  - 열 너비 자동 조정
  - 점수/별 수 숫자 형식
- [ ] 당첨자 중복 방지 옵션을 추가한다.
  - 한 번 뽑힌 사람 제외
  - 특정 그룹 제외
  - 관리자 수동 제외
- [ ] 추첨 룰을 더 추가한다.
  - TOP3 팀 모두에게 투표한 사람
  - 가장 큰 버블 작성자
  - 가장 많은 팀에 응원한 사람
  - 가장 많은 메시지를 남긴 사람
  - 1위 팀 응원자 중 추첨
- [ ] 행운권 추첨 화면을 로또/룰렛형 연출로 개선한다.
  - 화면 중앙 정렬
  - 대형 글자와 당첨자 강조
  - 발표자가 누르는 `멈춤` 버튼
  - 전체화면 송출 모드
  - 1부/2부 추첨 상태 구분
- [ ] Showup 화면에서 사회자가 클릭한 버블을 당첨 후보로 임시 저장할 수 있게 한다.
- [ ] 전체화면 모드 진입 버튼을 더 명확히 한다.
- [ ] 실시간 현황판을 발표장 전체화면에 맞게 조정한다.
  - 화면에 꽉 차는 레이아웃
  - 과도한 여백 제거
  - 관객 공개용 정보만 노출
- [ ] 모바일 짧은 주소 안내를 상단 또는 별도 QR 없는 안내 카드로 제공한다.
- [ ] 응원 메시지 버튼을 더 눈에 잘 띄게 개선한다.
- [ ] 관객 안내 문구를 더 명확하게 다듬는다.
  - 예: `별 10개를 응원하는 팀에 나눠 담으세요. 별과 함께 응원 메시지를 남기면 경품 추첨에 자동 응모됩니다.`

## P3. 코드 품질과 테스트

- [ ] Playwright e2e 테스트를 추가한다.
  - 등록
  - 별 배분
  - 별 회수
  - 응원 메시지 작성
  - 메시지 숨김 후 응모 조건 해제
  - 추첨 실행
  - 퀴즈 출제와 선착순 정답자 표시
  - XLSX 다운로드
- [ ] API 단위 테스트를 추가한다.
  - `normalizeAllocations`
  - participant identity normalization
  - team config validation
  - raffle candidate selection
- [ ] `src/App.tsx`를 역할별 컴포넌트 파일로 분리한다.
  - audience
  - admin
  - cheer showup
  - raffle
  - export
  - team config
- [ ] `server.mjs`와 `worker/index.ts`의 공통 로직을 공유 모듈로 분리한다.
- [ ] team config schema를 명시적으로 정의한다.
- [ ] XLSX 생성 로직을 별도 모듈로 분리한다.
- [ ] 로고 data URL 저장 용량 한계를 문서화하고, 큰 로고 업로드 시 안내 메시지를 개선한다.

## P4. 향후 확장

- [ ] 커스텀 도메인을 연결한다.
- [ ] Cloudflare Access로 관리자 인증을 붙인다.
- [ ] 행사별 room을 여러 개 만들 수 있게 한다.
- [ ] 과거 행사 결과 조회 화면을 추가한다.
- [x] 발표장 대형 스크린용 `/wall` 전용 route를 만든다.
- [ ] 팀별 최종 수상 결과 입력과 결과 리포트 생성 기능을 추가한다.
- [ ] 개인정보 보관/삭제 정책을 README와 운영 문서에 명시한다.

## 하지 않기로 한 것

- [ ] 사번, 이메일, 전화번호 기반 신원 확인은 기본 흐름에 넣지 않는다.
- [ ] 사진, 카메라, QR 스캔 기반 참여 확인은 기본 흐름에 넣지 않는다.
- [ ] 강한 디바이스 fingerprint는 기본 흐름에 넣지 않는다.
- [ ] 모든 관객이 LLM을 호출하는 prompt audition 기능은 이 MVP 범위에 넣지 않는다.
